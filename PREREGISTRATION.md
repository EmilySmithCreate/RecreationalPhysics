# Pre-registration

Empty on purpose. To be written in task T7 (`TASKS.md`) and committed **before** any run of the λ map. The git commit containing the final version is the timestamp.

It must state: the knob values; sizes; observables; what counts as first order, as continuous, and as inconclusive; and what outcome would count against the hypothesis in `VISION.md`.

The draft for the parked menu study is in `docs/parked/PREREGISTRATION_menu_study.md`.
